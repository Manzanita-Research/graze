# Gateway URL — leave empty to use Vite proxy (recommended for dev)
VITE_GATEWAY_URL=
# Webhook token from openclaw.json hooks.token
VITE_HOOK_TOKEN=
# Sync server WebSocket URL
VITE_SYNC_URL=ws://127.0.0.1:5858
# Room ID (all devices sharing the same room sync together)
VITE_ROOM_ID=clawpad-main
